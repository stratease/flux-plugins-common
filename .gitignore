# Dependencies
node_modules/
vendor/

# Build outputs
# Note: assets/js/dist/*.bundle.js files MUST be committed to the repository
# so they're available when the library is installed via Composer/Strauss.
# Only ignore source maps and temporary build files.
*.bundle.js.map
*.map

# IDE
.vscode/
.idea/
*.swp
*.swo
